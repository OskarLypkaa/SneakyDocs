{
  "name": "collapse-docs",
  "displayName": "Collapse Docs",
  "description": "Quickly collapse or uncollapse all Python docstrings via right-click menu.",
  "publisher": "Oskarlypka",
  "version": "1.1.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "activationEvents": [
    "onLanguage:python",
    "onCommand:collapseDocs.run",
    "onCommand:collapseDocs.unfold"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "collapseDocs.run",
        "title": "Collapse All Docstrings"
      },
      {
        "command": "collapseDocs.unfold",
        "title": "Uncollapse All Docstrings"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "collapseDocs.run",
          "when": "editorLangId == python",
          "group": "navigation"
        },
        {
          "command": "collapseDocs.unfold",
          "when": "editorLangId == python",
          "group": "navigation"
        }
      ]
    }
  },
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "icon": "images/icon.png",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "vscode": "^1.1.37"
  }
}
